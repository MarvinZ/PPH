import { ElementRef, Renderer, AfterViewInit, OnChanges, SimpleChanges, OnDestroy } from '@angular/core';
export declare abstract class BaseDirective implements AfterViewInit, OnChanges, OnDestroy {
    protected el: ElementRef;
    protected renderer: Renderer;
    value: string;
    protected key: string;
    private element;
    private renderNode;
    private nodeValue;
    private textObserver;
    private valueAttribute;
    private readonly MUTATION_CONFIG;
    constructor(el: ElementRef, renderer: Renderer);
    ngAfterViewInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    protected abstract setup(): void;
    protected abstract replace(): void;
    protected setText(value: string): void;
    private addTextListener();
    private removeTextListener();
    private getText();
    private getKey();
}
